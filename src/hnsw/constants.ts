/** The data space is the canvas itself: a vector *is* a point, and distance is
 *  literally the number of pixels between two dots. */
export const WORLD = { width: 1000, height: 640 } as const

export const MAX_LAYERS = 8
